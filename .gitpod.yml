tasks:
  - name: Setup & Install Dependencies
    init: |
      echo "🚀 Installing dependencies..."
      npm install
      echo "✓ Dependencies installed"
      echo "📦 Installing Playwright browsers..."
      npx playwright install --with-deps chromium firefox webkit
      echo "✓ Playwright browsers installed"
    command: |
      echo ""
      echo "╔════════════════════════════════════════════════════════════╗"
      echo "║  🎭 Playwright Test Automation - Gitpod Environment       ║"
      echo "╚════════════════════════════════════════════════════════════╝"
      echo ""
      echo "✅ Workspace is ready for testing!"
      echo ""
      echo "📋 Available Commands:"
      echo "  npm test              - Run all tests (headless)"
      echo "  npm run test:chrome   - Run tests in Chrome only"
      echo "  npm run test:firefox  - Run tests in Firefox only"
      echo "  npm run test:webkit   - Run tests in WebKit only"
      echo "  npm run test:report   - Run tests and show HTML report"
      echo ""
      echo "📊 View test results:"
      echo "  npx playwright show-report"
      echo ""
      echo "🔍 Debug tests:"
      echo "  npm run test:debug"
      echo ""

vscode:
  extensions:
    - ms-playwright.playwright@1.0.4
    - eamodio.gitlens@14.10.0
    - esbenp.prettier-vscode@10.1.0
    - dbaeumer.vscode-eslint@2.4.2
    - ms-vscode.makefile-tools@0.6.0

ports:
  - port: 3000
    onOpen: ignore
  - port: 9323
    onOpen: ignore

github:
  prebuilds:
    master: true
    branches: true
    pullRequests: true
    pullRequestsFromForks: false
    addComment: false
    addBadge: true
